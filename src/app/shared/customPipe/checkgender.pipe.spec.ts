import { CheckgenderPipe } from './checkgender.pipe';

describe('CheckgenderPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckgenderPipe();
    expect(pipe).toBeTruthy();
  });
});
